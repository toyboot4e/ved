#+TITLE: ved
#+STARTUP: indent

* Task
** DONE Decorate should add parse information
CLOSED: [2025-04-27 Sun 17:37]
** DONE Handle intersection decorations
CLOSED: [2025-04-27 Sun 17:37]
See hovering toolbar example
Do I have to parse manually?
- ruby
  Must not intersect with each other
  Inline block?
- bold, italic, ..
  Applied inside ruby etc
** DONE Use module augumentation
CLOSED: [2025-05-21 Wed 22:37]
https://docs.slatejs.org/concepts/12-typescript
** DONE Use newtype for position types
CLOSED: [2025-05-21 Wed 23:16]
Branded types
https://zenn.dev/farstep/articles/typescript-branded-types
** DONE =compilerOptions= not working
CLOSED: [2025-05-21 Wed 23:30]
Use =tsconfig.web.json=
** TODO Configure linter and formatter
** TODO Parse to rich text with position information
** TODO Export React components only for fast refresh
** TODO Fix cursor position on org-appear
** Use =nodeToDecorations= for caching?
It is a cache of decorations inside a code block element:
#+BEGIN_SRC ts
type EditorWithDecorations = CustomEditor & {
  nodeToDecorations: Map<Element, TokenRange[]>
}
#+END_SRC
* Technology
** Installation
#+BEGIN_SRC sh
$ npm i react react-dom @types/react @types/react-dom
$ npm i -D eslint-plugin-react eslint-plugin-react-hooks eslint-plugin-react-refresh
#+END_SRC
* React
** React + Electron
It's hard, so stick with =electron-vite=.
*** Copy assets on rebuild
*** IPC communication
** + Storybook
#+BEGIN_SRC sh
$ # TODO: vite-electron installation
$ npm install @storybook/builder-vite --save-dev
#+END_SRC
** Logging
User would want to check error logs.
* Development
** Windows チェック
*** IME は縦書き対応か
** 固定スタイルを作成する
*** スタイル
**** DONE =textarea= は段組みできなかった
CLOSED: [2025-04-12 Sat 23:01]
**** 最初の段落を右へ寄せる
*** Electron アプリの作り方について
**** html/css/js をどう配置するか、既存のアプリをあたってみる
*** ファイル保存機能を作成する
*** ファイル読み込み機能を作成する
** インラインスタイルを検討する
** 見た目を良くする
*** 行末を表示する
*** 常に次のブロックに下線を引く
** 無限スクロールモードを作る
** 見た目の変更
*** ルビをインライン表示する
カーソルの無い行でのみインライン表示する
*** 場面転換をインライン表示する
** Widgets
*** Bottom bar
*** Top bar
*** ファイル選択
ヘッダの内容をパースしてメニュー表示する
*** ツリービュー
*** Git 操作
** コンフィグ
*** ルビ文字の書式設定
*** 場面転換の書式設定

* Trashbox
** Custom insertText for correcting cursor position sync (failed)
#+BEGIN_SRC tsx
const withCustomInsertText = (editor: Editor) => {
  const { insertText: originalInsertText } = editor

  editor.insertText = (text: string) => {
    console.log('custom insert', text)
    const { selection } = editor
    // TODO: collapsed ではない場合も処理が必要
    if (selection && Range.isCollapsed(selection)) {
      // TODO: markdown-shortcuts.tsx のように Editor.above で見た方が良くない？
      const [selectionStart, selectionEnd] = Range.edges(selection)
      console.log('sel', selectionStart, selectionEnd)

      const [node, _path] = Editor.node(editor, selectionStart.path)
      // TODO: この分岐必要か？
      if (Text.isText(node)) {
        // 非 appear 中の非表示文字を数える
        const { anchor } = selection

        let deltaOffset = 0
        const ranges: VedRange[] = []
        decorateRubies(editor, ranges, selectionStart.path, node.text)
        for (let range of ranges) {
          if (range.anchor.offset >= anchor.offset) {
            // after the cursor
            break
          }
          if (range.focus.offset > anchor.offset) {
            // appearing
            break
          }
          const format = range.format
          const textLen = format.text[1] - format.text[0]
          const wholeLen = range.focus.offset - range.anchor.offset
          deltaOffset += wholeLen - textLen
        }

        const correctedPoint = {
          path: selectionStart.path,
          offset: selectionStart.offset - deltaOffset
        }

        console.log('fix', selectionStart.offset, correctedPoint.offset)
        // TODO: use originalInsertText?
        Transforms.insertText(editor, text, {
          at: correctedPoint
        })

        return
      } else {
        console.log('not text')
      }
    }

    // Use the original insertText method as a fallback
    originalInsertText(text)
  }

  return editor
}
#+END_SRC
** TODO
